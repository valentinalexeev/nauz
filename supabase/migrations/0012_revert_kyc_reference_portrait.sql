-- Откат 0011: биометрическая переверификация через сохранённый портрет
-- оказалась платной доп-услугой у Didit — вместо неё формальное согласие
-- (чекбокс на форме) + переиспользование уже одобренной верификации того
-- же пользователя (см. src/lib/voices/start-kyc.ts), без нового обращения
-- к провайдеру и без хранения биометрии.
--
-- Бакет storage 'kyc-portraits' из 0011 намеренно не удаляется здесь —
-- прямое удаление строк storage.buckets запрещено защитой от потери данных
-- (storage.protect_delete). Бакет пуст (в него ничего не успело загрузиться)
-- и просто остаётся неиспользуемым; при желании его можно удалить вручную
-- через дашборд Supabase или Storage API.
alter table public.profiles drop column if exists kyc_reference_portrait_path;
